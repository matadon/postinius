message = MailMessage.new(data)

--

message = MailMessage.new
message.is_reply_to(other_message)

--

message.attach(file)
message.attach(:data => data, :filename => '', :mimetype => '..')

--

# Dump the raw serialized message out.
message.raw

--

# Shortcut for multipart replies.
message.text = 
message.html = 

--

message.multipart do
    part do
    end
end

--
